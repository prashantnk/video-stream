{
  "name": "apiserver",
  "version": "1.0.0",
  "description": "\"uses json server for hosting json file of streams\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1" ,
    "start": "json-server -p 3001 --watch db.json"
  },
  "author": "\"prashant ranjan\"",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
